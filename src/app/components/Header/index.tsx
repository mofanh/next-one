const Header = () => {
    return (
        <header className="header">
            {/* 这里可以添加你想要在Header中展示的内容，比如导航栏、logo等 */}
            <h1>NextChat</h1>
        </header>
    );
};

export default Header;